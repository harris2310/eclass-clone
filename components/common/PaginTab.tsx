/*
Mayb will use in the future

import React from "react";

type Props = { selected: boolean; num: number };

const PaginTab = React.forwardRef(({ selected, num }: Props), ref) => {
  return (
    <>
      <button
        className={
          selected
            ? " p-2 m-2 bg-cyan-600 text-white"
            : " text-black p-2 m-2 border border-grey-400"
        }
      >
        {num}
      </button>
    </>
  );
}

export {};
*/

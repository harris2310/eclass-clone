import React from "react";

type Props = {};

const Footer = (props: Props) => {
  return (
    <div className=" text-center font-semibold text-xl py-4 bg-gray-200">
      @duth
    </div>
  );
};

export default Footer;
